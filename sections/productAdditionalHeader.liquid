{%- liquid
  comment
    'Assign the background color. It will try to use a product metafield with the key'
    ' "second_accent_color" first. If that metafield is not set on the product,'
    ' it will fall back to the color selected in the theme editor settings for this section.'
  endcomment
  assign bg_color = product.metafields.custom.second_accent_color | default: section.settings.background_color
-%}

{% comment %}
  This section creates a header that is initially in the page flow and becomes
  sticky when the user scrolls down to it.

  - It appears in the theme layout where it is placed.
  - It uses JavaScript to create a "clone" of itself that is appended to
    the end of the <body> tag. This clone is what becomes the sticky header.
  - A scroll listener checks the position of the original section and toggles
    the visibility of the clone when the original reaches the top of the viewport.
{% endcomment %}

<div class="custom-sticky-product-header-wrapper" data-section-id="{{ section.id }}">
  <div class="footer-container">
    <div class="footer-left">
      <h2 class="product-title-bottom-footer">{{ product.title | truncate: 35 }}</h2>
      <div class="footer-menu">
        <a href="#science" class="footer-link">Science</a>
        <a href="#reviews" class="footer-link">Reviews</a>
        <a href="#ingredients" class="footer-link">Ingredients</a>
        <a href="#dosage" class="footer-link">Dosage</a>
        <a href="#faq" class="footer-link">FAQ</a>
        <a href="#subscription" class="footer-link">Subscription</a>
      </div>
    </div>
    <div class="footer-actions">
      <div class="footer-price-section">
        {% if product.compare_at_price > product.price %}
          <span class="footer-price-old">{{ product.compare_at_price | money }}</span>
        {% endif %}
        <span class="footer-price-current">{{ product.price | money }}</span>
      </div>
      {%- form 'product', product, id: 'sticky-product-form', class: 'sticky-form' -%}
        <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
        <button type="submit" name="add" class="add-to-cart-button">
          <span>Add To Cart</span>
        </button>
      {%- endform -%}
    </div>
  </div>
</div>

<style>
  /* This is the original section, which acts as the trigger/placeholder in the document flow */
  .custom-sticky-product-header-wrapper {
    background-color: {{ bg_color }};
    color: {{ section.settings.text_color }};
  }

  /* This is the CLONED element that appears at the top */
  .custom-sticky-product-header-wrapper.is-sticky-clone {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 100;
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);
    transform: translateY(-100%);
    transition: transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    display: none; /* Initially hidden until JS takes over */
  }

  /* This class makes the CLONE visible with a smooth animation */
  .custom-sticky-product-header-wrapper.is-sticky-clone.is-visible {
    transform: translateY(0);
  }

  /* Shared styles for both the original and the clone */
  .footer-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 12px 24px;
    max-width: 1200px;
    margin: 0 auto;
  }

  .footer-left {
    display: flex;
    align-items: center;
    gap: 24px;
  }
  
  .product-title-bottom-footer {
    font-size: 1.1rem;
    font-weight: 600;
    margin: 0;
    white-space: nowrap;
  }
  
  .footer-menu {
    display: flex;
    gap: 16px;
  }

  .footer-link {
    font-size: 0.9rem;
    text-decoration: none;
    color: inherit;
    opacity: 0.8;
    transition: opacity 0.2s ease;
  }
  
  .footer-link:hover {
    opacity: 1;
    text-decoration: underline;
  }

  .footer-actions {
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .footer-price-section {
    display: flex;
    align-items: baseline;
    gap: 8px;
  }

  .footer-price-old {
    text-decoration: line-through;
    opacity: 0.7;
    font-size: 0.9rem;
  }
  
  .footer-price-current {
    font-size: 1.1rem;
    font-weight: 700;
    color: {{ section.settings.price_color }};
  }
  
  .add-to-cart-button {
    background-color: {{ section.settings.button_color }};
    color: {{ section.settings.button_text_color }};
    border: none;
    padding: 12px 24px;
    border-radius: 8px;
    cursor: pointer;
    font-weight: 600;
    font-size: 1rem;
    transition: background-color 0.2s ease;
  }
  
  .add-to-cart-button:hover {
    filter: brightness(1.1);
  }

  /* Responsive Styles */
  @media (max-width: 768px) {
    .footer-menu, .product-title-bottom-footer {
      display: none;
    }
    .footer-container {
      justify-content: center;
    }
    .footer-left {
       display: none;
    }
    .footer-actions {
      width: 100%;
      justify-content: space-between;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const sectionId = '{{ section.id }}';
    const originalWrapper = document.querySelector(`.custom-sticky-product-header-wrapper[data-section-id="${sectionId}"]`);

    if (!originalWrapper) {
      console.log('Sticky header original wrapper not found. Exiting.');
      return;
    }

    // 1. Clone the original element to create our sticky version
    const stickyClone = originalWrapper.cloneNode(true);
    stickyClone.classList.add('is-sticky-clone');
    stickyClone.style.display = 'block'; // Make it visible for JS control
    
    // 2. Append the clone to the body to escape theme containers
    document.body.appendChild(stickyClone);

    // 3. Set up the scroll listener
    const handleScroll = () => {
      // Get the position of the ORIGINAL element which acts as our trigger
      const triggerPoint = originalWrapper.getBoundingClientRect().top;
      
      // When the top of the original element hits the top of the viewport...
      if (triggerPoint <= 0) {
        // ...show the clone.
        stickyClone.classList.add('is-visible');
      } else {
        // ...hide the clone.
        stickyClone.classList.remove('is-visible');
      }
    };
    
    // Attach the scroll listener
    window.addEventListener('scroll', handleScroll, { passive: true });
    
    // Run an initial check in case the page loads already scrolled down
    handleScroll();
  });
</script>

{% schema %}
{
  "name": "Sticky Product Header",
  "settings": [
    {
      "type": "header",
      "content": "Color Settings"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Fallback Background Color",
      "info": "Used if the product metafield 'Second Accent Color' is not set.",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#1F2937"
    },
    {
      "type": "color",
      "id": "price_color",
      "label": "Price Color",
      "default": "#1F2937"
    },
    {
      "type": "color",
      "id": "button_color",
      "label": "Button Background",
      "default": "#3B82F6"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button Text Color",
      "default": "#FFFFFF"
    }
  ],
  "presets": [
    {
      "name": "Sticky Product Header"
    }
  ]
}
{% endschema %}


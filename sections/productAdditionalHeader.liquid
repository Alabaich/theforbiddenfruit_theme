{% comment %}
  This section creates a header that is initially in the page flow and becomes
  sticky when the user scrolls down to it.

  - It appears in the theme layout where it is placed.
  - It uses JavaScript to create a "clone" of itself that is appended to
    the end of the <body> tag. This clone is what becomes the sticky header.
  - A scroll listener checks the position of the original section and toggles
    the visibility of the clone when the original reaches the top of the viewport.
{% endcomment %}

<div class="custom-sticky-product-header-wrapper" data-section-id="{{ section.id }}">
  <div class="sticky-footer-container">
    <div class="sticky-footer-left">
      <h2 class="sticky-product-title-bottom-footer">{{ product.title | truncate: 35 }}</h2>
      <div class="sticky-footer-menu">
        <a href="#science" class="sticky-footer-link">Science</a>
        <a href="#reviews" class="sticky-footer-link">Reviews</a>
        <a href="#ingredients" class="sticky-footer-link">Ingredients</a>
        <a href="#dosage" class="sticky-footer-link">Dosage</a>
        <a href="#faq" class="sticky-footer-link">FAQ</a>
        <a href="#subscription" class="sticky-footer-link">Subscription</a>
      </div>
    </div>
    <div class="sticky-footer-actions">
      <div class="sticky-footer-price-section">
        {% if product.compare_at_price > product.price %}
          <span class="sticky-footer-price-old">{{ product.compare_at_price | money }}</span>
        {% endif %}
        <span class="sticky-footer-price-current">{{ product.price | money }}</span>
      </div>
      {%- form 'product', product, id: 'sticky-product-form', class: 'sticky-form' -%}
        <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
        <button type="submit" name="add" class="sticky-add-to-cart-button">
          <span>Add To Cart</span>
        </button>
      {%- endform -%}
    </div>
  </div>
</div>

<style>
  /* This is the original section, which acts as the trigger/placeholder in the document flow */
  .custom-sticky-product-header-wrapper {
    background-color: {{ section.settings.background_color }};
    color: {{ section.settings.text_color }};
  }

  /* This is the CLONED element that appears at the top */
  .custom-sticky-product-header-wrapper.is-sticky-clone {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 100;
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);
    transform: translateY(-100%);
    transition: transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    display: none; /* Initially hidden until JS takes over */
  }

  /* This class makes the CLONE visible with a smooth animation */
  .custom-sticky-product-header-wrapper.is-sticky-clone.is-visible {
    transform: translateY(0);
  }

  /* Shared styles for both the original and the clone */
  .sticky-footer-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 12px 24px;
    max-width: 1200px;
    margin: 0 auto;
  }

  .sticky-footer-left {
    display: flex;
    align-items: center;
    gap: 24px;
  }
  
  .sticky-product-title-bottom-footer {
    font-size: 1.1rem;
    font-weight: 600;
    margin: 0;
    white-space: nowrap;
  }
  
  .sticky-footer-menu {
    display: flex;
    gap: 16px;
  }

  .sticky-footer-link {
    font-size: 0.9rem;
    text-decoration: none;
    color: inherit;
    opacity: 0.8;
    transition: opacity 0.2s ease;
  }
  
  .sticky-footer-link:hover {
    opacity: 1;
    text-decoration: underline;
  }

  .sticky-footer-actions {
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .sticky-footer-price-section {
    display: flex;
    align-items: baseline;
    gap: 8px;
  }

  .sticky-footer-price-old {
    text-decoration: line-through;
    opacity: 0.7;
    font-size: 0.9rem;
  }
  
  .sticky-footer-price-current {
    font-size: 1.1rem;
    font-weight: 700;
    color: {{ section.settings.price_color }};
  }
  
  .sticky-add-to-cart-button {
    background-color: #fff;
    color: #2c2d2c;
    border: none;
    padding: 12px 24px;
    border-radius: 8px;
    cursor: pointer;
    font-weight: 600;
    font-size: 1rem;
    transition: background-color 0.2s ease;
  }
  
  .sticky-add-to-cart-button:hover {
    filter: brightness(1.1);
  }

  /* Responsive Styles */
  @media (max-width: 768px) {
    .sticky-footer-menu, .sticky-product-title-bottom-footer {
      display: none;
    }
    .sticky-footer-container {
      justify-content: center;
    }
    .sticky-footer-left {
       display: none;
    }
    .sticky-footer-actions {
      width: 100%;
      justify-content: space-between;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const sectionId = '{{ section.id }}';
    const originalWrapper = document.querySelector(`.custom-sticky-product-header-wrapper[data-section-id="${sectionId}"]`);

    if (!originalWrapper) {
      console.log('Sticky header original wrapper not found. Exiting.');
      return;
    }

    // 1. Clone the original element to create our sticky version
    const stickyClone = originalWrapper.cloneNode(true);
    stickyClone.classList.add('is-sticky-clone');
    stickyClone.style.display = 'block'; // Make it visible for JS control
    
    // 2. Append the clone to the body to escape theme containers
    document.body.appendChild(stickyClone);

    // 3. Set up the scroll listener
    const handleScroll = () => {
      // Get the position of the ORIGINAL element which acts as our trigger
      const triggerPoint = originalWrapper.getBoundingClientRect().top;
      
      // When the top of the original element hits the top of the viewport...
      if (triggerPoint <= 0) {
        // ...show the clone.
        stickyClone.classList.add('is-visible');
      } else {
        // ...hide the clone.
        stickyClone.classList.remove('is-visible');
      }
    };
    
    // Attach the scroll listener
    window.addEventListener('scroll', handleScroll, { passive: true });
    
    // Run an initial check in case the page loads already scrolled down
    handleScroll();
  });
</script>

{% schema %}
{
  "name": "Sticky Product Header",
  "settings": [
    {
      "type": "header",
      "content": "Color Settings"
    }
  ],
  "presets": [
    {
      "name": "Sticky Product Header"
    }
  ]
}
{% endschema %}

